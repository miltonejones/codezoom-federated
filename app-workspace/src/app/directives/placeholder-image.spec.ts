import { PlaceholderImage } from './placeholder-image';

describe('PlaceholderImage', () => {
  it('should create an instance', () => {
    const directive = new PlaceholderImage();
    expect(directive).toBeTruthy();
  });
});
